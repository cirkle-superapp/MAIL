# Task ID: PREMIUM-UI-v3-TASK1

**Agent:** full-stack-developer
**Task:** Bring the Cirkle Mail email-list surface (middle pane) up to the world-class premium standard established by the v2 design system overhaul — header/toolbar, inbox tabs, date buckets, rows, bulk toolbar, skeleton, empty state.

## Files edited
- `/home/z/my-project/src/components/mail/email-list.tsx` (container, header, tabs strip, InboxTabButton, DateGroupedEmailList, BulkToolbar, EmailListSkeleton, EmptyState)
- `/home/z/my-project/src/components/mail/email-row.tsx` (EmailRow li className, Avatar, hover overlay, important dot, intent badge, label chips; added `index?` prop + internal stagger mapping)

## Work Log
1. Read `worklog.md` for the PREMIUM-UI-v2 design system tokens (`glass`, `glass-strong`, `bg-gradient-gold`, `bg-gradient-hero`, `shadow-soft/glow/premium`, `card-premium`, `btn-premium`, `animate-spring-in`, `animate-float`, `animate-pulse-glow`, `stagger-1..6`, `font-display`, `text-cream`, `text-gold`).
2. Read `globals.css` to confirm every utility class referenced by the spec is defined as a component (`@layer components`) or utility (`@layer utilities`) — all verified present.
3. **email-row.tsx**: added `index?: number` to `EmailRowProps`, declared a `STAGGER_CLASSES` constant, computed `staggerClass` via index-based cycling (no template literals — only `cn()`); restructured the `<li>` className so each state has its own bg+hover branch (active keeps `bg-primary/10` + adds `shadow-soft`; selected keeps `bg-muted/60`; unread gets `bg-gold/5 hover:bg-gold/10`; default gets `bg-muted/30 hover:bg-muted/50`); inset gold/primary accent bars preserved; `animate-spring-in` + `staggerClass` appended. Avatar ring upgraded to `transition-all duration-300 hover:ring-gold/40` (kept `group-hover:scale-105`). Hover overlay upgraded from `bg-background/95 ... shadow-soft backdrop-blur-md` to `glass-strong ... shadow-premium` (no longer manually specifying backdrop blur since `glass-strong` already provides it). Important dot gained `animate-pulse-glow`. Intent badge rounded + padded + `shadow-soft` (`rounded-full px-2 py-0.5 shadow-soft`). Label chips likewise (`rounded-full px-2 py-0.5 shadow-soft`).
4. **email-list.tsx**: container gained `relative`; list header upgraded to `glass animate-spring-in flex h-14 ... border-b border-border/40`; the bare `meta.icon` was wrapped in a `h-7 w-7 rounded-lg shadow-soft` chip with `bg-gradient-gold text-cream` (Inbox) or `bg-primary/10 text-primary` (everything else); title switched to `font-display text-base font-medium`; count converted to a `rounded-full bg-muted px-2 py-0.5 text-[11px] font-medium` pill; refresh & MoreVertical buttons upgraded with `btn-premium rounded-full`; the empty-trash/spam button gained `btn-premium ... rounded-full ... hover:text-destructive`. Inbox tabs strip converted to `glass ... border-b border-border/40 ... py-2`. InboxTabButton rewritten as a pill: `rounded-full px-3 py-1.5 transition-all duration-200`, active = `bg-primary/10 text-primary font-semibold shadow-soft`, inactive = `text-muted-foreground hover:bg-muted/60 hover:text-foreground` (removed `rounded-t-lg border-b-2`). Date bucket header upgraded to `glass-strong sticky top-0 z-10 flex h-9 ... border-b border-border/40`, prepended with a `h-3 w-1 rounded-full bg-gradient-gold` accent bar; bucket name styled `text-[11px] font-semibold uppercase tracking-wider text-foreground/70`; count rendered in a `ml-2 rounded-full bg-muted px-1.5 py-0.5 text-[10px] font-medium text-muted-foreground` chip. `EmailRow` is now passed `index={idx}` so it can stagger internally; the bucket `<ul>` divider softened to `divide-border/40`. Bulk toolbar wrapper gained `animate-spring-in`, count text upgraded to `font-display text-sm font-semibold text-foreground`, dividers softened to `bg-border/60`, every action button got `btn-premium rounded-full`. EmailListSkeleton: divide softened to `divide-border/40` and avatar skeleton gained `shadow-soft`. EmptyState wrapped in a `card-premium animate-spring-in w-full max-w-sm p-8` card; icon container changed to `rounded-2xl shadow-glow animate-float` with `bg-gradient-gold` (folder empty) or `bg-gradient-hero` (search empty); icon text color set to `text-cream`; title upgraded to `font-display text-base font-medium`; description to `text-sm text-muted-foreground`.
5. **No logic, state, handlers, or imports were modified.** Only className strings, JSX wrapping (the new icon-chip div), and the optional `index` prop on `EmailRow`. All existing functionality (select-all, bulk actions, snooze/label menus, undo toasts, refresh, MoreVertical, empty-trash confirm dialog, date grouping, hover action overlay, important dot, intent badges, label chips, checkbox/star, mobile density, keyboard nav) is unchanged.
6. Ran `bun run lint` → EXIT 0, no errors. Dev server confirmed still serving the page (`GET / 200`).

## Stage Summary
- The email-list middle pane now matches the premium v3 standard set by the Command Center / Top Bar: glass header with gradient icon chip + pill count; glass pill-tabs with shadow-soft; glass-strong date buckets with a gold accent bar; refined rows with subtle gold-tinted unread state, gold hover accent, spring-in staggered entrance, premium avatar ring, premium glass hover overlay, pulse-glow important dot, fully rounded+shadowed intent & label chips; glass bulk toolbar with `font-display` count + `btn-premium rounded-full` actions + spring entrance; premium skeleton with shadowed avatars; and a `card-premium` empty-state with a gradient icon that floats and glows.
- Files changed: `src/components/mail/email-list.tsx`, `src/components/mail/email-row.tsx`.
- Lint status: `bun run lint` passes clean (EXIT 0, 0 errors, 0 warnings). Dev server running on port 3000 (`GET / 200`).
- No breaking changes — every existing interaction preserved.
